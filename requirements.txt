fastf1>=3.3
pandas>=2.2.3,<3.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
jupyter>=1.0
matplotlib>=3.7
seaborn>=0.12

# v2 (Grid Prophet v2 spec, phase 1): hierarchical Bayesian pace/reliability
# model + challenger. pandas is pinned >=2.2.3 here because pandas 2.2.2
# (numpy1-era wheel) plus a numpy>=2 environment corrupts the XGBoost/LightGBM
# C extension call path on Windows -- see src/model/challenger.py docstring.
pymc>=5.0
arviz>=0.17
